<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_select_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/widget_select_overlay"
    android:padding="24dp"
    android:gravity="center">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/widget_select_card_bg"
        android:padding="20dp"
        android:elevation="6dp"
        android:clickable="true"
        android:focusable="true">

        <TextView
            android:id="@+id/select_list_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/widget_select_title"
            android:textColor="@color/widget_task_title"
            android:textSize="20sp"
            android:fontFamily="sans-serif-medium"
            android:paddingBottom="12dp" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/widget_divider"
            android:layout_marginBottom="12dp" />

        <ListView
            android:id="@+id/widget_list_select_items"
            android:layout_width="match_parent"
            android:layout_height="320dp"
            android:divider="@color/widget_divider"
            android:dividerHeight="1dp"
            android:scrollbars="vertical" />

        <androidx.appcompat.widget.SwitchCompat
            android:id="@+id/widget_select_show_all_time"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/widget_select_show_all_time_label"
            android:textColor="@color/widget_task_title"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium"
            android:layout_marginTop="8dp" />

        <TextView
            android:id="@+id/widget_select_show_all_time_hint"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/widget_task_title_done"
            android:textSize="12sp"
            android:fontFamily="sans-serif"
            android:layout_marginTop="2dp"
            android:layout_marginBottom="12dp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end"
            android:layout_marginTop="4dp">

            <Button
                android:id="@+id/widget_select_cancel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@android:string/cancel"
                android:textColor="@color/widget_task_title_done"
                android:background="?android:attr/selectableItemBackground"
                style="?android:attr/borderlessButtonStyle" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>
